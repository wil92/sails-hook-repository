let chai = require('chai');
let expect = chai.expect;

describe('Base test', function () {

    before(() => {
        // do something
    });

    after(() => {
        // do something
    });

    it("first test", () => {
        expect(true).to.be.true;
    });
});
